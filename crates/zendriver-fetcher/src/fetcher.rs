//! Public [`Fetcher`] entry point.
//!
//! Stub — populated in Phase 5 Task 21.

/// Chrome for Testing binary downloader.
///
/// Stub — public builder + `ensure_chrome` land in Task 21.
#[derive(Debug, Default)]
#[non_exhaustive]
pub struct Fetcher;

impl Fetcher {
    /// Construct a new fetcher with default options.
    pub fn new() -> Self {
        Self
    }
}
